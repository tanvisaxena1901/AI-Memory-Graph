server:
  port: 8081

spring:
  application:
    name: aegis-telemetry-service

aegis:
  cors:
    allowed-origins: ${AEGIS_ALLOWED_ORIGINS:http://localhost:5174,http://localhost:5173,http://127.0.0.1:5174,https://tanvisaxena1901.github.io}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
