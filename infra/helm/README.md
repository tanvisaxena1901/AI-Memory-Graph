# Helm

Phase 1 keeps raw Kubernetes manifests under `infra/kubernetes`.

Add a chart here once service contracts settle:

- `aegis-api-gateway`
- `aegis-ai-engine`
- OpenSearch dependency values
- Neo4j dependency values
- PostgreSQL dependency values
